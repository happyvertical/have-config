# HappyVertical scope publishes to GitHub Packages.
# Authentication comes from NODE_AUTH_TOKEN (set by CI's setup-node step,
# or by your user-level ~/.npmrc with a PAT that has read:packages scope).
@happyvertical:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
