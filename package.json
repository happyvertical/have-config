{
  "name": "have-config",
  "version": "0.0.0",
  "private": true,
  "description": "HappyVertical shared cross-repo configuration (workspace root).",
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "changeset": "changeset",
    "changeset:auto": "tsx scripts/auto-changeset.ts",
    "changeset:version": "changeset version",
    "changeset:publish": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.0",
    "tsx": "^4.20.0"
  }
}
