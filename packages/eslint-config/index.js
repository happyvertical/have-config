/**
 * @happyvertical/eslint-config
 *
 * Default export is the base TypeScript config. Named imports are
 * available for the Svelte preset:
 *
 *   import config from '@happyvertical/eslint-config';        // = base
 *   import base from '@happyvertical/eslint-config/base';
 *   import svelte from '@happyvertical/eslint-config/svelte';
 */

export { default } from './base.js';
